{
  "title": "Multi-Cloud: Azure & GCP Fundamentals",
  "phase": "cloud",
  "week": 2,
  "category": "Cloud",
  "difficulty": "intermediate",
  "duration": "~75 min",
  "prerequisites": ["cloud-w1-aws"],
  "summary": "AWS, Azure, and GCP offer equivalent core services under different names and with different trade-offs. Every DevOps engineer works across multiple clouds — either because their employer uses one, or because they're deploying to customers who use different clouds. This lesson maps AWS concepts to Azure and GCP equivalents, covers each cloud's managed Kubernetes service (EKS/AKS/GKE), and introduces multi-cloud patterns and tooling that works across all three.",
  "sections": [
    {
      "id": "cloud-comparison",
      "title": "AWS vs Azure vs GCP: the core services map",
      "type": "lesson",
      "body": "The three major cloud providers — AWS, Azure, and GCP — are often called **hyperscalers** because of their global infrastructure scale. Every DevOps engineer will encounter all three throughout their career: some employers standardise on one, but customers, acquired companies, and best-of-breed service choices frequently pull teams across cloud boundaries. The skill of quickly mapping concepts from one cloud to another is therefore invaluable.\n\n**Why they all exist and why each has market share** comes down to origins and strengths. AWS launched in 2006, the first public cloud, and has the largest catalogue of services and the deepest ecosystem of third-party tooling. Microsoft Azure grew from Azure Active Directory and deep integration with the Microsoft stack — Windows Server, Active Directory, Office 365, Visual Studio — making it the natural choice for enterprises already invested in Microsoft technology. Google Cloud Platform was built by the engineers who created Kubernetes, MapReduce, and the global network backbone; its strengths are networking quality, Kubernetes (GKE is the reference implementation), and data/analytics services like BigQuery.\n\n**The core insight** is that every cloud provides the same fundamental building blocks: virtual machines, object storage, managed Kubernetes, managed databases, secret management, container registries, and identity systems. They just use different names, different pricing models, and different organisational hierarchies.\n\n**Resource organisation differs significantly** across clouds. In AWS, resources belong to an Account, grouped logically by tags or resource groups, networked within a VPC. In Azure, resources belong to a Subscription, organised into Resource Groups (mandatory containers), and networked within a Virtual Network (VNet). In GCP, resources belong to a Project, which is both the billing boundary and the IAM boundary, networked within a VPC that is global by default.\n\n**Identity and access management philosophy** also varies. AWS IAM is resource-centric: policies attach to users, groups, or roles and reference specific resource ARNs. Azure uses Azure Active Directory (now Entra ID) which is an enterprise identity system first, with Azure RBAC layered on top. GCP uses Cloud IAM with a hierarchical model where permissions granted at the organisation level flow down to folders, projects, and resources.\n\n**The three hyperscalers — different names, same concepts:**\n\n```\nCategory          AWS                    Azure                  GCP\n─────────────────────────────────────────────────────────────────────────────\nCompute\n  VMs             EC2                    Virtual Machines       Compute Engine\n  Containers      ECS / Fargate          Container Instances    Cloud Run\n  Kubernetes      EKS                    AKS                    GKE\n  Serverless      Lambda                 Functions              Cloud Functions\n  PaaS            Elastic Beanstalk      App Service            App Engine\n\nStorage\n  Object          S3                     Blob Storage           Cloud Storage\n  Block           EBS                    Managed Disks          Persistent Disk\n  File            EFS                    Azure Files            Filestore\n  Archive         S3 Glacier             Archive Blob           Cloud Storage Coldline\n\nDatabase\n  Relational      RDS (PostgreSQL/MySQL)  Azure Database         Cloud SQL\n  Managed PG      Aurora PostgreSQL       Flexible Server        AlloyDB\n  NoSQL           DynamoDB               Cosmos DB              Firestore / Bigtable\n  Cache           ElastiCache (Redis)     Azure Cache for Redis  Memorystore\n  Data Warehouse  Redshift               Synapse Analytics      BigQuery\n\nNetworking\n  VPC             VPC                    Virtual Network (VNet) VPC\n  DNS             Route 53               Azure DNS              Cloud DNS\n  CDN             CloudFront             Azure CDN / Front Door  Cloud CDN\n  Load Balancer   ALB / NLB              Application Gateway     Cloud Load Balancing\n  Private Link    PrivateLink            Private Endpoint        Private Service Connect\n  Peering         VPC Peering            VNet Peering           VPC Peering\n\nIdentity & Security\n  IAM             IAM                    Azure AD / Entra ID    Cloud IAM\n  Secrets         Secrets Manager        Key Vault              Secret Manager\n  Certificate     ACM                    Key Vault Certs        Certificate Manager\n  OIDC workloads  IRSA (IAM Roles SA)    Workload Identity      Workload Identity\n\nCI/CD & DevOps\n  Registry        ECR                    ACR                    Artifact Registry\n  CI/CD           CodePipeline           Azure DevOps           Cloud Build\n  GitOps          CodeCommit             Azure Repos            Cloud Source Repos\n\nObservability\n  Metrics         CloudWatch             Azure Monitor          Cloud Monitoring\n  Logs            CloudWatch Logs        Log Analytics          Cloud Logging\n  Tracing         X-Ray                  Application Insights   Cloud Trace\n```\n\n**Key differences in philosophy:**\n\n```\nAWS:    Largest service catalog (~250 services), most mature, most complex IAM\n        Best for: breadth, enterprise compliance, migration from on-prem\n\nAzure:  Deep Microsoft integration (Active Directory, Office 365, Visual Studio)\n        Best for: Microsoft shops, hybrid cloud (Azure Arc), enterprise Windows\n\nGCP:    Best networking (global VPC, premium tier routing), best data services\n        Kubernetes-native (Google invented it), best price/performance for\n        compute. Best for: Kubernetes workloads, data/ML, startups\n```"
    },
    {
      "id": "azure-core",
      "title": "Azure core services for DevOps",
      "type": "lesson",
      "body": "Azure's design philosophy reflects its origin as an enterprise platform built on top of Microsoft's existing directory and identity infrastructure. Before you can provision a single resource in Azure, you must understand its resource hierarchy — it is more opinionated than AWS but also more consistent in how it enforces governance.\n\n**Azure Subscriptions** are the billing boundary. An organisation typically has multiple subscriptions: one per environment (dev, staging, production), one per business unit, or one per major product. Subscriptions are grouped under a **Management Group** hierarchy that allows policy enforcement at scale using Azure Policy.\n\n**Resource Groups** are the fundamental organisational unit within a subscription. Every resource — a VM, an AKS cluster, a storage account, a Key Vault — must belong to a resource group. A resource group has a region (though the resources inside it can span regions), and it is the unit of deployment, access control, and deletion. This is simultaneously more structured than AWS (which has no equivalent mandatory grouping) and more intuitive for lifecycle management: deleting a resource group deletes all resources inside it.\n\n**Azure Kubernetes Service (AKS)** is Azure's managed Kubernetes. Like EKS, Azure manages the control plane at no extra charge (as of 2023). AKS integrates deeply with Azure Active Directory for RBAC, with Azure Container Registry (ACR) for image pulls, and with Azure Monitor for observability. The key differentiator is **Azure Arc**, which lets you attach non-Azure Kubernetes clusters (on-premises, other clouds) to Azure management — useful for hybrid deployments.\n\n**Azure Workload Identity** is the equivalent of AWS IRSA. It uses OIDC federation between AKS and Azure AD so that pods can authenticate to Azure services (Key Vault, Storage, Service Bus) using a managed identity annotation on the ServiceAccount — no secrets stored in the cluster.\n\n**Azure Key Vault** is the secret management service, equivalent to AWS Secrets Manager. The **CSI driver** integration mounts secrets directly into pod filesystems, so your application reads a file instead of calling an API — a simpler integration pattern than using an SDK.\n\n**Azure resource model — everything is inside a Resource Group:**\n\n```\nAzure Subscription\n  └── Resource Group (logical container — like an AWS account/region bucket)\n        ├── Virtual Network\n        ├── Virtual Machines\n        ├── AKS Cluster\n        ├── Storage Account\n        └── Key Vault\n\n# AWS analogy: Resource Group ≈ VPC + loose grouping of resources\n# GCP analogy: Resource Group ≈ Project\n```\n\n**Azure CLI — equivalent to AWS CLI:**\n\n```bash norun\n# Login\naz login\n\n# Set active subscription\naz account set --subscription \"My Subscription\"\n\n# Create a resource group (required before creating anything)\naz group create --name myapp-rg --location eastus\n\n# AKS — Azure Kubernetes Service\naz aks create \\\n  --resource-group myapp-rg \\\n  --name myapp-cluster \\\n  --node-count 3 \\\n  --node-vm-size Standard_D2s_v3 \\\n  --enable-managed-identity \\\n  --enable-oidc-issuer \\\n  --enable-workload-identity \\\n  --generate-ssh-keys\n\n# Get kubeconfig\naz aks get-credentials --resource-group myapp-rg --name myapp-cluster\n\n# Azure Container Registry\naz acr create \\\n  --resource-group myapp-rg \\\n  --name myappregistry \\\n  --sku Basic\n\n# Attach ACR to AKS (grants AKS pull access)\naz aks update --name myapp-cluster --resource-group myapp-rg \\\n  --attach-acr myappregistry\n```\n\n**Azure Workload Identity — pods authenticate to Azure services without secrets:**\n\n```yaml\n# Equivalent to AWS IRSA (IAM Roles for Service Accounts)\n# The pod gets a managed identity, no long-lived credentials needed\n\napiVersion: v1\nkind: ServiceAccount\nmetadata:\n  name: myapp\n  namespace: production\n  annotations:\n    # Azure AD application client ID\n    azure.workload.identity/client-id: \"<MANAGED_IDENTITY_CLIENT_ID>\"\nlabels:\n  azure.workload.identity/use: \"true\"\n```\n\n**Azure Key Vault + CSI Driver — mount secrets as files:**\n\n```yaml\n# SecretProviderClass — maps Key Vault secrets to pod files\napiVersion: secrets-store.csi.x-k8s.io/v1\nkind: SecretProviderClass\nmetadata:\n  name: azure-kvname\nspec:\n  provider: azure\n  parameters:\n    usePodIdentity: \"false\"\n    useVMManagedIdentity: \"true\"\n    keyvaultName: \"myapp-keyvault\"\n    objects: |\n      array:\n        - |\n          objectName: db-password\n          objectType: secret\n    tenantId: \"<TENANT_ID>\"\n```\n\n**Azure Blob Storage (equivalent to S3):**\n\n```bash norun\n# Create storage account\naz storage account create \\\n  --name myappstorage \\\n  --resource-group myapp-rg \\\n  --sku Standard_LRS \\\n  --kind StorageV2\n\n# Create a container (equivalent to S3 bucket)\naz storage container create \\\n  --account-name myappstorage \\\n  --name artifacts\n\n# Upload a file\naz storage blob upload \\\n  --account-name myappstorage \\\n  --container-name artifacts \\\n  --name myfile.tar.gz \\\n  --file ./myfile.tar.gz\n```"
    },
    {
      "id": "gcp-core",
      "title": "GCP core services for DevOps",
      "type": "lesson",
      "body": "Google Cloud Platform was built by the engineers who created Kubernetes, the global fibre network backbone (B4), MapReduce, and Bigtable. This heritage shows in its architecture: GCP's networking is widely regarded as technically superior to AWS and Azure, its Kubernetes Engine (GKE) is the reference implementation of managed Kubernetes, and its data services (BigQuery, Dataflow, Pub/Sub) have no equivalent in scale or simplicity on other clouds.\n\n**GCP Projects** are the fundamental organisational unit — unlike AWS's Account or Azure's Resource Group, a GCP Project is simultaneously the billing boundary, the IAM boundary, and the resource namespace. When you enable an API (e.g. the Kubernetes Engine API, the Secret Manager API), you enable it per project. This makes projects feel more like AWS accounts than like resource groups: you typically create one project per environment (dev, staging, prod) or per team.\n\n**GCP's global VPC** is a key architectural differentiator. In AWS, a VPC is regional — you create separate VPCs in each region and peer them manually. In GCP, a single VPC spans all regions by default. You create subnets in specific regions, but routing between regions is automatic over Google's private fibre backbone without traversing the public internet. This simplifies multi-region architectures significantly.\n\n**GKE (Google Kubernetes Engine)** is the most mature managed Kubernetes offering. Google invented Kubernetes and ran it internally for years before open-sourcing it, so GKE tends to support new Kubernetes features earliest. Autopilot mode is particularly notable: GKE manages not just the control plane but also the nodes, automatically scaling them to fit your pods. You pay per pod's CPU and memory request, not per node.\n\n**GCP Workload Identity** lets pods authenticate to GCP services (Cloud Storage, Secret Manager, Pub/Sub) without any credential files. A Kubernetes ServiceAccount is bound to a GCP Service Account via an IAM policy binding; the GKE metadata server issues short-lived tokens to pods automatically. The same pattern as AWS IRSA and Azure Workload Identity, but the specific setup steps differ.\n\n**BigQuery** deserves special mention because it has no real equivalent on other clouds. It is a serverless, fully managed data warehouse that can query petabytes of data using standard SQL in seconds. There is no infrastructure to provision, no cluster to size, and no index to manage. You pay per TB of data scanned (or a flat monthly rate for reservations). Teams that move analytics from on-premises or AWS Redshift to BigQuery routinely report 10x improvements in query performance at lower cost.\n\n**GCP resource hierarchy — Projects, not accounts:**\n\n```\nOrganization (company.com)\n  └── Folder (optional grouping)\n        └── Project  ← the billing and IAM boundary (like an AWS account)\n              ├── GKE Cluster\n              ├── Cloud Storage buckets\n              ├── Cloud SQL instances\n              └── Artifact Registry\n\n# AWS analogy: GCP Project ≈ AWS Account\n# The project is also the IAM boundary — permissions are per-project\n```\n\n**gcloud CLI:**\n\n```bash norun\n# Login\ngcloud auth login\n\n# Set project\ngcloud config set project my-project-id\n\n# GKE — Google Kubernetes Engine (the best managed k8s)\ngcloud container clusters create myapp-cluster \\\n  --zone us-central1-a \\\n  --num-nodes 3 \\\n  --machine-type e2-standard-2 \\\n  --enable-workload-identity \\\n  --workload-pool=my-project-id.svc.id.goog\n\n# Get kubeconfig\ngcloud container clusters get-credentials myapp-cluster --zone us-central1-a\n\n# Artifact Registry (successor to Container Registry)\ngcloud artifacts repositories create myapp \\\n  --repository-format=docker \\\n  --location=us-central1\n\n# Tag and push an image\ndocker tag myapp:latest \\\n  us-central1-docker.pkg.dev/my-project/myapp/myapp:latest\ndocker push us-central1-docker.pkg.dev/my-project/myapp/myapp:latest\n```\n\n**GCP Workload Identity — pods use service accounts without key files:**\n\n```yaml\n# Bind a Kubernetes ServiceAccount to a GCP Service Account\napiVersion: v1\nkind: ServiceAccount\nmetadata:\n  name: myapp\n  namespace: production\n  annotations:\n    iam.gke.io/gcp-service-account: myapp@my-project.iam.gserviceaccount.com\n```\n\n```bash norun\n# Allow the k8s SA to impersonate the GCP SA (IAM binding)\ngcloud iam service-accounts add-iam-policy-binding \\\n  myapp@my-project.iam.gserviceaccount.com \\\n  --role roles/iam.workloadIdentityUser \\\n  --member \"serviceAccount:my-project.svc.id.goog[production/myapp]\"\n```\n\n**GCP Secret Manager:**\n\n```bash norun\n# Create a secret\necho -n 'my-db-password' | gcloud secrets create db-password --data-file=-\n\n# Access from a pod (using Application Default Credentials via Workload Identity)\npython3 -c \"\nfrom google.cloud import secretmanager\nclient = secretmanager.SecretManagerServiceClient()\nname = 'projects/my-project/secrets/db-password/versions/latest'\nresponse = client.access_secret_version(request={'name': name})\nprint(response.payload.data.decode('UTF-8'))\n\"\n```\n\n**BigQuery — GCP's data warehouse (no AWS/Azure equivalent):**\n\n```bash norun\n# BigQuery is unique: serverless SQL on petabyte scale, no infrastructure to manage\nbq query --use_legacy_sql=false \\\n  'SELECT COUNT(*) as total, status\n   FROM my_project.analytics.requests\n   WHERE DATE(timestamp) = CURRENT_DATE()\n   GROUP BY status'\n\n# Process a TB of data in seconds — you only pay for the data scanned\n```"
    },
    {
      "id": "multi-cloud-patterns",
      "title": "Multi-cloud patterns and cloud-agnostic tooling",
      "type": "lesson",
      "body": "Multi-cloud is both a strategy and a complexity multiplier. Understanding when it genuinely helps versus when it creates unnecessary overhead is a senior DevOps skill. The good news is that cloud-agnostic tooling has matured to the point where the Kubernetes layer is largely portable — the same application manifests, Helm charts, and GitOps workflows deploy identically on EKS, AKS, and GKE.\n\n**The portability stack** works from the bottom up. Terraform uses provider plugins to abstract cloud-specific resources: you write HCL modules once and instantiate them with an AWS, Azure, or GCP backend. Helm charts deploy on any Kubernetes cluster regardless of where it runs. ArgoCD, cert-manager, External Secrets Operator, and Prometheus work identically across clouds. The only cloud-specific layer is the SecretStore (which cloud's secret manager to read from), the container registry URL, and the workload identity annotations on ServiceAccounts.\n\n**External Secrets Operator (ESO)** deserves special attention as a multi-cloud unifier. ESO introduces two CRDs: `SecretStore` (which secret backend to connect to, with cloud-specific configuration) and `ExternalSecret` (which secrets to fetch and how to map them to Kubernetes Secrets). The `ExternalSecret` is identical regardless of cloud — only the `SecretStore` changes. This means your application manifests and Helm charts can be cloud-agnostic; only the infrastructure layer (the SecretStore) differs per cloud.\n\n**When multi-cloud genuinely helps:** a customer contractually requires data residency in a specific cloud; you want disaster recovery across providers as an extreme availability measure; your team is building a SaaS product that must deploy into customer-owned cloud accounts (which may be any of the three). These are legitimate, well-defined use cases.\n\n**When multi-cloud creates more problems than it solves:** premature adoption before mastering one cloud deeply; cost arbitrage that rarely justifies the operational overhead; \"avoiding vendor lock-in\" on commodity services like object storage that all clouds provide equivalently and cheaply. The teams that suffer most from multi-cloud are the ones that adopted it for the wrong reasons before they had the staff to operate it properly.\n\n**Cloud-agnostic tooling — write once, run anywhere:**\n\n```\nTerraform    → infrastructure provisioning across all three clouds\n             AWS provider:   registry.terraform.io/hashicorp/aws\n             Azure provider: registry.terraform.io/hashicorp/azurerm\n             GCP provider:   registry.terraform.io/hashicorp/google\n\nHelm         → same charts deploy on EKS, AKS, GKE\n             Cloud-specific values in values-aws.yaml, values-azure.yaml, etc.\n\nArgoCD       → works on any k8s cluster regardless of cloud\nExternal Secrets → supports AWS Secrets Manager, Azure Key Vault, GCP Secret Manager\nCert-manager → supports Let's Encrypt, Vault, all cloud CAs\nCrossplane   → provision cloud resources from Kubernetes CRDs\n```\n\n**Terraform multi-cloud pattern:**\n\n```hcl\n# Use workspaces or separate state files per cloud\n# backend.tf\nterraform {\n  backend \"s3\" {\n    bucket = \"mycompany-tfstate\"\n    key    = \"aws/production/terraform.tfstate\"\n    region = \"us-east-1\"\n  }\n  required_providers {\n    aws   = { source = \"hashicorp/aws\",     version = \"~> 5.0\" }\n    azurerm = { source = \"hashicorp/azurerm\", version = \"~> 3.0\" }\n    google = { source = \"hashicorp/google\",  version = \"~> 5.0\" }\n  }\n}\n\n# Abstract cloud-specific resources with modules\nmodule \"k8s_cluster\" {\n  source = \"./modules/k8s-cluster\"\n  cloud  = var.cloud   # \"aws\" | \"azure\" | \"gcp\"\n  ...\n}\n```\n\n**External Secrets Operator — unified secret management:**\n\n```yaml\n# Same ESO CRD works across all clouds — only the SecretStore changes\n\n# AWS:\napiVersion: external-secrets.io/v1beta1\nkind: SecretStore\nspec:\n  provider:\n    aws:\n      service: SecretsManager\n      region: us-east-1\n\n# Azure:\napiVersion: external-secrets.io/v1beta1\nkind: SecretStore\nspec:\n  provider:\n    azurekv:\n      vaultUrl: https://my-vault.vault.azure.net\n\n# GCP:\napiVersion: external-secrets.io/v1beta1\nkind: SecretStore\nspec:\n  provider:\n    gcpsm:\n      projectID: my-project-id\n```\n\n**Multi-cloud cost comparison (approximate, 2026):**\n\n```\nResource              AWS              Azure            GCP\n──────────────────────────────────────────────────────────────\n2 vCPU, 8GB RAM      t3.large $0.083/h D2s_v3 $0.096/h  e2-standard-2 $0.067/h\nManaged PostgreSQL   db.t3.medium      General Purpose   db-standard-2\n  (2vCPU, 4GB)       $0.068/h          $0.13/h           $0.104/h\nObject storage       S3 $0.023/GB      Blob $0.018/GB    GCS $0.020/GB\nManaged k8s (mgmt)   EKS $0.10/h       AKS Free          GKE $0.10/h\nEgress (first 10TB)  $0.09/GB          $0.087/GB         $0.08/GB\n\nConclusion: GCP typically 10-20% cheaper for compute; Azure for Microsoft shops;\nAWS for the widest service catalog and enterprise compliance needs.\n```\n\n**When to go multi-cloud:**\n\n```\nGood reasons:\n  - Customer requirement: \"we only allow data in Azure\"\n  - Avoid vendor lock-in on managed services\n  - Disaster recovery across providers\n  - Best-of-breed: GCP for ML (Vertex AI), AWS for compliance, Azure for AD integration\n\nBad reasons:\n  - \"We should hedge against a cloud going down\" (all three have 99.9%+ uptime)\n  - Premature: multi-cloud doubles operational complexity before you've mastered one\n  - Cost arbitrage: savings rarely justify the complexity\n\nRecommendation: master one cloud deeply first, then expand.\n```"
    },
    {
      "id": "exercise",
      "title": "Exercise: map AWS commands to Azure and GCP equivalents",
      "type": "exercise",
      "body": "One of the most effective ways to internalise multi-cloud knowledge is to map the same conceptual operation across all three CLIs. Each cloud has its own CLI tool (`aws`, `az`, `gcloud`), but they follow similar patterns: a top-level noun, a verb, required flags for resource identity, and optional flags for filtering output.\n\n**The cloud-agnostic Kubernetes layer** is where the real multi-cloud story lives in modern DevOps. Once you have a kubeconfig pointing at an EKS, AKS, or GKE cluster, `kubectl` commands are identical. The Kubernetes API does not care which cloud the control plane runs on. This is why Kubernetes won the container orchestration wars: it became a portable abstraction layer above cloud-specific compute.\n\n**External Secrets Operator** is already installed in this sandbox, giving you a hands-on example of the cloud-agnostic secret pattern. In a real environment you would point the SecretStore at your cloud's secret manager. Here, we simulate it with a Kubernetes Secret as the backend.\n\n**Reading real-world Dockerfiles and docker-compose.yml files** for multi-cloud projects is a skill in itself — notice how image names, environment variable patterns, and health check endpoints are designed to be cloud-agnostic, while deployment scripts and CI workflows contain the cloud-specific plumbing.\n\nPractice the CLI equivalents across clouds without needing live cloud accounts — using the local cluster as context.\n\n```bash norun\n# ── 1. Explore the cluster's cloud-agnostic resources ────────────────────\n# External Secrets Operator is already installed. Check the SecretStore:\nkubectl get secretstore -A\nkubectl describe secretstore -n learning\n\n# ── 2. Examine the ExternalSecret for DB credentials ─────────────────────\nkubectl get externalsecrets -A\nkubectl describe externalsecret -n learning\n\n# ── 3. Review the Terraform structure for cloud-agnostic patterns ─────────\ncat << 'EOF'\n# This pattern works across all three clouds:\n# - Same Kubernetes manifests (Deployment, Service, Ingress)\n# - Same Helm charts\n# - Same ArgoCD application\n# Only the SecretStore provider changes per cloud\n\n# AWS:\n# provider.aws.service: SecretsManager\n\n# Azure:\n# provider.azurekv.vaultUrl: https://my-vault.vault.azure.net\n\n# GCP:\n# provider.gcpsm.projectID: my-project-id\nEOF\n\n# ── 4. Compare managed Kubernetes CLI commands ────────────────────────────\ncat << 'EOF'\n# ── EKS (AWS) ──\n# Create cluster:\n# eksctl create cluster --name prod --region us-east-1 --nodes 3\n# Get kubeconfig:\n# aws eks update-kubeconfig --name prod --region us-east-1\n\n# ── AKS (Azure) ──\n# Create cluster:\n# az aks create -g myapp-rg -n prod --node-count 3\n# Get kubeconfig:\n# az aks get-credentials -g myapp-rg -n prod\n\n# ── GKE (GCP) ──\n# Create cluster:\n# gcloud container clusters create prod --zone us-central1-a --num-nodes 3\n# Get kubeconfig:\n# gcloud container clusters get-credentials prod --zone us-central1-a\nEOF\n\n# ── 5. Practice cloud-agnostic secret access pattern ─────────────────────\n# Create a secret the 'cloud-agnostic' way using ESO\nkubectl create secret generic simulated-backend-store \\\n  --from-literal=api-key=cloud-agnostic-demo-key \\\n  -n learning 2>/dev/null || true\n\n# Apply an ExternalSecret that reads from the fake store\nkubectl apply -n learning -f - << 'YAML'\napiVersion: external-secrets.io/v1beta1\nkind: ExternalSecret\nmetadata:\n  name: cloud-demo-secret\nspec:\n  refreshInterval: 1h\n  secretStoreRef:\n    name: local-store\n    kind: SecretStore\n  target:\n    name: cloud-demo-secret\n    creationPolicy: Owner\n  data:\n  - secretKey: api-key\n    remoteRef:\n      key: simulated-backend-store\n      property: api-key\nYAML\n\n# ── 6. Review what k8s resources are cloud-agnostic ──────────────────────\nkubectl api-resources | grep -E 'Deployment|Service|Ingress|HPA|PDB'\n# All of these work identically on EKS, AKS, and GKE\n\n# ── 7. Clean up ──────────────────────────────────────────────────────────\nkubectl delete externalsecret cloud-demo-secret -n learning 2>/dev/null || true\nkubectl delete secret simulated-backend-store cloud-demo-secret -n learning 2>/dev/null || true\n```"
    },
    {
      "id": "quiz",
      "title": "Quiz: multi-cloud",
      "type": "quiz",
      "questions": [
        {
          "q": "What is the GCP equivalent of AWS S3 object storage?",
          "options": [
            "GCP Blob Storage",
            "Cloud Storage",
            "GCP Object Store",
            "Persistent Disk"
          ],
          "answer": 1,
          "explanation": "Google Cloud Storage is the GCP equivalent of AWS S3. Like S3, it provides durable, scalable object storage with multiple storage classes (Standard, Nearline, Coldline, Archive). The Azure equivalent is Azure Blob Storage."
        },
        {
          "q": "What is the AWS IAM equivalent on Azure for a pod needing to access cloud resources without storing credentials?",
          "options": [
            "Azure Active Directory Application Secrets",
            "Azure Managed Identity with Workload Identity federation",
            "Azure Service Principal with certificate",
            "Kubernetes Secrets with Azure credentials"
          ],
          "answer": 1,
          "explanation": "Azure Workload Identity (using Managed Identities) is the Azure equivalent of AWS IRSA (IAM Roles for Service Accounts). A Kubernetes ServiceAccount is annotated with a managed identity client ID, and AKS uses OIDC federation to allow the pod to authenticate to Azure services without any stored credentials."
        },
        {
          "q": "Which tool provides a unified interface for provisioning resources across AWS, Azure, and GCP?",
          "options": [
            "kubectl",
            "Ansible",
            "Terraform",
            "Helm"
          ],
          "answer": 2,
          "explanation": "Terraform has providers for all major cloud platforms and uses a single HCL language to describe infrastructure across clouds. The AWS, Azure, and GCP providers are all maintained by HashiCorp. Helm handles Kubernetes application packaging (cloud-agnostic at the k8s layer), but Terraform handles the underlying cloud infrastructure."
        }
      ]
    },
    {
      "id": "challenge",
      "title": "Challenge: design a multi-cloud deployment",
      "type": "challenge",
      "goal": "Design and document a deployment strategy for a web application that needs to run on both AWS and Azure, sharing the same Kubernetes manifests but with cloud-specific secret management and container registries.",
      "hints": [
        "Start by listing what differs per cloud (registry URL, SecretStore provider, IAM annotations) vs what stays the same (Deployment, Service, HPA, NetworkPolicy)",
        "Use Helm values files to handle cloud differences: `values-aws.yaml` and `values-azure.yaml`",
        "External Secrets Operator's SecretStore is the only resource that needs to change — ExternalSecret and the app manifests stay identical",
        "For the container registry: use `imageRegistry` as a Helm value — `aws_account.dkr.ecr.region.amazonaws.com/myapp` vs `myregistry.azurecr.io/myapp`"
      ],
      "success_criteria": [
        "A Helm chart exists with a `values.yaml`, `values-aws.yaml`, and `values-azure.yaml`",
        "The chart uses `imageRegistry` and `secretStore.provider` as configurable values",
        "Two SecretStore manifests exist (one for each cloud provider) in separate overlays or value files",
        "The core Deployment, Service, and HPA are identical between both cloud configurations",
        "A README explains how to deploy to each cloud: `helm install ... -f values-aws.yaml` vs `values-azure.yaml`"
      ]
    }
  ]
}

{
  "title": "Distributed Tracing & OpenTelemetry",
  "phase": "observability",
  "week": 3,
  "category": "Observability",
  "difficulty": "advanced",
  "duration": "~80 min",
  "prerequisites": ["obs-w1-prometheus", "obs-w2-logs"],
  "summary": "Distributed tracing reveals the full journey of a request as it hops across services, databases, and queues. While metrics say something is slow and logs say what failed, traces show exactly where time is spent and which service is responsible. OpenTelemetry is the industry standard instrumentation SDK; Jaeger and Grafana Tempo are the most common backends.",
  "sections": [
    {
      "id": "what-is-tracing",
      "title": "What distributed tracing solves",
      "type": "lesson",
      "body": "## What is distributed tracing and why does it exist?\n\nIn a monolithic application, understanding where a slow request spends its time is straightforward: add a timer around each function call, or attach a profiler. The entire execution happens in one process on one machine, so the call stack tells the complete story. Microservices architecture changes this fundamentally. A single user-facing request may fan out across an API gateway, an authentication service, an order service, a database, an inventory service, and a cache — each running as a separate process, possibly on separate nodes, writing logs to separate streams.\n\nWhen that request is slow, metrics tell you *that* it is slow but not *where* in the chain the time is being spent. Logs from each service exist in isolation — you can find the entry in the order service logs, but connecting it to the corresponding entry in the inventory service logs requires manually correlating timestamps and IDs, which is error-prone and slow under production pressure. **Distributed tracing** solves this by recording a structured, hierarchical timeline — called a **trace** — of every operation the request triggers, across every service, as a single queryable unit.\n\nTracing fits into the broader observability picture as the third pillar alongside metrics and logs. The three pillars are complementary: metrics provide aggregate health indicators (error rate, p99 latency), logs provide the detailed narrative of individual events, and traces provide the causal map showing how work flows across service boundaries and where time is spent. In a well-instrumented system, an on-call engineer can move from a firing alert (metrics) → the error log entry → the full trace showing the slow database call, all within a single Grafana session.\n\nBefore running the code in this section, understand that distributed tracing requires **instrumentation** — explicit code (or auto-instrumentation middleware) that creates spans and propagates context across service calls. It does not happen automatically; someone must add the OTel SDK to each service.\n\nIn a monolith, a slow request is easy to find — add a timer, check the stack trace. In microservices, a single user request might touch 10+ services. Logs from each service exist, but correlating them manually is painful.\n\n**The three pillars of observability:**\n\n```\nMetrics  → WHAT is broken (error rate 15%, p99 latency 8s)\nLogs     → WHY it broke (\"DB connection pool exhausted\")\nTraces   → WHERE time is spent across all services\n```\n\n**Core tracing concepts:**\n\n```\nTrace     — the complete journey of one request, end-to-end\n            Identified by a trace_id (e.g., 4bf92f3577b34da6)\n\nSpan      — a single unit of work within a trace\n            Has: name, start_time, duration, attributes, events\n            Example spans in one request:\n              → HTTP handler (120ms total)\n                → authenticate_user (5ms)\n                → db.query users (80ms)\n                  → postgres.execute (78ms)\n                → render_response (2ms)\n\nParent/child — spans form a tree showing call hierarchy\nContext propagation — trace_id flows across service boundaries\n                      via HTTP headers (traceparent, X-B3-TraceId)\n```\n\n**What a trace looks like in a UI:**\n\n```\nTrace: GET /api/orders/123      duration: 245ms\n│\n├── api-gateway                  0ms ─────────────────────────── 245ms\n│   ├── auth-service             5ms ───── 18ms\n│   ├── order-service            22ms ──────────────────── 195ms\n│   │   ├── postgres query       25ms ──────────── 110ms\n│   │   │   └── connection pool  26ms ─── 42ms     (waited 16ms!)\n│   │   └── inventory-service    115ms ──── 145ms\n│   └── cache write              198ms ─── 205ms\n```\n\nThis immediately shows: the bottleneck is the database (110ms) and connection pool wait (16ms), not the inventory service."
    },
    {
      "id": "opentelemetry",
      "title": "OpenTelemetry: the instrumentation standard",
      "type": "lesson",
      "body": "## What is OpenTelemetry and why does the industry standardise on it?\n\nBefore OpenTelemetry, distributed tracing suffered from a fragmentation problem. Every tracing backend — Jaeger, Zipkin, AWS X-Ray, Datadog, Honeycomb — provided its own proprietary instrumentation library. If your application used the Jaeger SDK and you wanted to switch to Honeycomb, you had to rewrite all your instrumentation code. If you ran a polyglot system (Python services, Go services, Java services), you needed to learn and maintain a different SDK per language per backend. Vendor lock-in at the instrumentation layer was real and painful.\n\nOpenTelemetry (OTel) is a CNCF project — a merger of the earlier OpenCensus and OpenTracing projects — that solves this by providing a **single, vendor-neutral API and SDK** for generating telemetry data (traces, metrics, and logs). You instrument your application once using the OTel API, and then configure an exporter to send data to whichever backend you choose. Switching backends requires only changing the exporter configuration, not rewriting instrumentation.\n\nThe OTel **Collector** is a particularly important component. Rather than having each application instance send telemetry directly to a backend, you deploy a Collector as a sidecar or as a cluster-level deployment. The Collector receives spans via the OTLP protocol, can apply sampling decisions (drop 99% of successful fast requests, keep 100% of errors), batch them for efficiency, and fan out to multiple backends simultaneously. This separation means your application code has no direct dependency on the backend, and you can change sampling strategies or add new backends without redeploying applications.\n\nOpenTelemetry (OTel) is a CNCF project that provides a single, vendor-neutral SDK for traces, metrics, and logs. Before OTel, you had to choose: Jaeger SDK, Zipkin SDK, AWS X-Ray SDK — each incompatible.\n\n**OTel architecture:**\n\n```\nYour Application\n    │ OTel SDK (traces + metrics + logs)\n    │\n    ▼\nOTel Collector (optional but recommended)\n    │ receives via OTLP (gRPC or HTTP)\n    │ can filter, sample, batch, re-export\n    │\n    ├──▶ Jaeger    (trace UI)\n    ├──▶ Tempo     (Grafana-native trace backend)\n    ├──▶ Zipkin    (lightweight alternative)\n    └──▶ Datadog, Honeycomb, etc.\n```\n\n**Key OTel concepts:**\n\n```python\nfrom opentelemetry import trace\nfrom opentelemetry.sdk.trace import TracerProvider\nfrom opentelemetry.sdk.trace.export import BatchSpanProcessor\nfrom opentelemetry.exporter.otlp.proto.grpc.trace_exporter import OTLPSpanExporter\n\n# 1. Set up provider\nprovider = TracerProvider()\n\n# 2. Add exporter (where to send spans)\nexporter = OTLPSpanExporter(endpoint=\"http://otel-collector:4317\")\nprovider.add_span_processor(BatchSpanProcessor(exporter))\ntrace.set_tracer_provider(provider)\n\n# 3. Get a tracer\ntracer = trace.get_tracer(\"my-service\")\n\n# 4. Create spans\nwith tracer.start_as_current_span(\"process-order\") as span:\n    span.set_attribute(\"order.id\", order_id)\n    span.set_attribute(\"user.id\", user_id)\n    \n    result = process(order_id)\n    \n    span.set_attribute(\"order.status\", result.status)\n    span.add_event(\"order processed\", {\"items\": len(result.items)})\n```\n\n**Context propagation — how trace_id crosses service boundaries:**\n\n```python\nimport requests\nfrom opentelemetry.propagate import inject\n\n# Service A calling Service B\nwith tracer.start_as_current_span(\"call-inventory\") as span:\n    headers = {}\n    inject(headers)  # injects traceparent header automatically\n    \n    response = requests.get(\n        \"http://inventory-service/api/stock\",\n        headers=headers  # Service B receives this and continues the trace\n    )\n\n# The traceparent header looks like:\n# traceparent: 00-4bf92f3577b34da6a3ce929d0e0e4736-00f067aa0ba902b7-01\n#              version  trace_id                          span_id         flags\n```\n\n**Sampling strategies:**\n\n```\nAlways-on sampling     — record every request\n                         Fine for low-traffic services, expensive at scale\n\nHead-based sampling    — decide at the start (before seeing the trace)\n                         e.g., sample 10% of requests\n                         Problem: misses rare errors in the 90%\n\nTail-based sampling    — decide at the end (after seeing duration/errors)\n                         Sample 100% of errors + slow requests, 1% of fast ones\n                         Requires OTel Collector or Tempo backend support\n                         This is what production systems should use\n```"
    },
    {
      "id": "jaeger-tempo",
      "title": "Jaeger and Grafana Tempo",
      "type": "lesson",
      "body": "## What are trace backends and how do you choose between them?\n\nOnce your application is instrumented with OpenTelemetry and spans are flowing out via OTLP, you need somewhere to store and query them. That is the role of the **trace backend**. The backend receives spans, stores them indexed by trace ID, and provides a UI and query API for finding and visualising individual traces. Two backends dominate the self-hosted open-source space: Jaeger (from Uber, donated to CNCF) and Grafana Tempo (from Grafana Labs).\n\nThe key architectural difference is storage. Jaeger traditionally used Cassandra or Elasticsearch as its storage backend — established databases that provide fast indexed lookups, but at significant operational cost and storage overhead. **Grafana Tempo** was designed from the ground up to use cheap object storage (S3, GCS) as its primary store, with only a small in-memory or local-disk index of trace IDs. This makes Tempo dramatically cheaper to run at scale — traces are stored at object storage prices rather than SSD RAID prices. The trade-off is that Tempo does not support full-text search across span attributes; you must know the trace ID or use TraceQL with specific attribute filters.\n\nGrafana Tempo's tighter integration with the Grafana ecosystem is also significant. When you look at a Loki log entry that contains a trace ID, Grafana can automatically render a button that opens the corresponding Tempo trace. When you look at a Tempo span for a service, Grafana can jump to the Prometheus metrics for that service at the exact time window. This **exemplar-based correlation** (Prometheus recording trace IDs alongside metric samples) creates a genuinely seamless debugging workflow that would require custom integration work with other stacks.\n\nFor new deployments, Tempo is generally the better choice when Grafana is already in use. Jaeger remains widely deployed in organisations that adopted it before Tempo existed and have not yet migrated.\n\n**Jaeger** — Uber's open-source tracing system, donated to CNCF.\n\n```\nComponents:\n  jaeger-agent       — receives spans from app (UDP, sidecar or DaemonSet)\n  jaeger-collector   — validates, transforms, stores spans\n  jaeger-query       — serves the UI and API\n  Storage backend    — Cassandra, Elasticsearch, or in-memory (dev)\n```\n\n```bash norun\n# All-in-one dev mode (single binary, in-memory):\ndocker run -d --name jaeger \\\n  -p 16686:16686 \\\n  -p 4317:4317 \\\n  jaegertracing/all-in-one:latest\n# UI: http://localhost:16686\n```\n\n**Grafana Tempo** — Grafana's trace backend, designed for cost efficiency.\n\n```\nAdvantages over Jaeger:\n  - Object storage backend (S3/GCS) — cheap at scale\n  - Integrates natively with Grafana dashboards\n  - TraceQL: query language for traces (like PromQL for metrics)\n  - Metrics generator: converts trace data into Prometheus metrics\n\nDisadvantages:\n  - Newer, less mature than Jaeger\n  - No built-in sampling decisions (relies on OTel Collector)\n```\n\n**TraceQL (Tempo's query language):**\n\n```\n# Find slow spans\n{ duration > 500ms }\n\n# Find spans with errors in a specific service\n{ .service.name = \"order-service\" && status = error }\n\n# Find database spans over 100ms\n{ span.db.system = \"postgresql\" && duration > 100ms }\n\n# Find traces where a specific user had an error\n{ .user.id = \"1234\" && status = error }\n```\n\n**Correlating traces with logs and metrics in Grafana:**\n\n```\n1. Add trace_id to every log line\n   logger.info(\"order processed\", extra={\"trace_id\": get_current_span().get_span_context().trace_id})\n\n2. In Grafana Explore:\n   - Search Loki for logs with trace_id\n   - Click the trace_id → jump to Tempo\n   - From Tempo span → jump to Prometheus metrics for that service\n\nThis is the \"golden path\" for production debugging:\n  Alert fires → open Grafana → look at metrics to understand scope\n  → look at logs to find error message\n  → click trace_id → see exactly which call was slow\n```"
    },
    {
      "id": "instrumentation-exercise",
      "title": "Exercise: instrument a Python service with OpenTelemetry",
      "type": "exercise",
      "body": "## What you will practise\n\nInstrumentation is a skill you develop by writing it, not by reading about it. This exercise walks through instrumenting a realistic Python service — one with nested function calls that represent a real request lifecycle (authentication, database query, response rendering). You will use the **ConsoleSpanExporter**, which writes completed spans to stdout as JSON. This is the standard way to develop and verify instrumentation locally before connecting to a real backend like Tempo or Jaeger.\n\nPay close attention to two things as you work through this exercise. First, notice how the **parent-child span relationship** is established automatically by the Python context manager: when you open a new span inside an existing `with tracer.start_as_current_span(...)` block, OTel automatically sets the outer span as the parent of the inner span. This is how the call hierarchy in a trace UI is built — it comes from the nested Python context, not from any explicit parent ID you pass. Second, notice how **span attributes** differ from log fields: attributes are structured key-value pairs attached to a specific span, queryable in TraceQL as `{ .db.statement = '...' }`, rather than being embedded in a text string.\n\nThe error-tracking section at the end is equally important. `span.record_exception(e)` captures the full stack trace inside the span, which means a trace in Tempo can show you not just that a span errored, but the exact Python exception and traceback — without needing to separately search logs.\n\nInstall the OTel SDK and instrument a sample Python service. We'll send traces to stdout (console exporter) for visibility without needing a backend.\n\n```bash\n# ── 1. Install OpenTelemetry SDK ──────────────────────────────────────────\npip3 install opentelemetry-sdk opentelemetry-api --quiet\n\n# ── 2. Write an instrumented service ─────────────────────────────────────\ncat > /workspace/traced-service.py << 'PYEOF'\nimport time\nimport random\nfrom opentelemetry import trace\nfrom opentelemetry.sdk.trace import TracerProvider\nfrom opentelemetry.sdk.trace.export import ConsoleSpanExporter, BatchSpanProcessor\nfrom opentelemetry.sdk.resources import Resource\n\n# Configure provider with service name\nresource = Resource.create({\"service.name\": \"order-service\", \"service.version\": \"1.0.0\"})\nprovider = TracerProvider(resource=resource)\nprovider.add_span_processor(BatchSpanProcessor(ConsoleSpanExporter()))\ntrace.set_tracer_provider(provider)\n\ntracer = trace.get_tracer(\"order-service\")\n\ndef authenticate_user(user_id: int) -> bool:\n    with tracer.start_as_current_span(\"authenticate_user\") as span:\n        span.set_attribute(\"user.id\", user_id)\n        time.sleep(random.uniform(0.005, 0.015))\n        span.set_attribute(\"auth.method\", \"jwt\")\n        return True\n\ndef query_database(query: str) -> list:\n    with tracer.start_as_current_span(\"db.query\") as span:\n        span.set_attribute(\"db.system\", \"postgresql\")\n        span.set_attribute(\"db.statement\", query)\n        latency = random.uniform(0.02, 0.15)\n        time.sleep(latency)\n        if latency > 0.1:\n            span.add_event(\"slow query detected\", {\"threshold_ms\": 100, \"actual_ms\": int(latency * 1000)})\n        return [{\"id\": 1, \"total\": 99.99}]\n\ndef process_order(order_id: int, user_id: int):\n    with tracer.start_as_current_span(\"process_order\") as span:\n        span.set_attribute(\"order.id\", order_id)\n        span.set_attribute(\"user.id\", user_id)\n\n        authenticated = authenticate_user(user_id)\n        if not authenticated:\n            span.set_status(trace.StatusCode.ERROR, \"auth failed\")\n            return None\n\n        orders = query_database(f\"SELECT * FROM orders WHERE id = {order_id}\")\n        span.set_attribute(\"order.items_count\", len(orders))\n        span.add_event(\"order retrieved\", {\"order_id\": order_id})\n        return orders\n\n# Simulate 3 requests\nfor i in range(3):\n    print(f\"\\n--- Processing order {i+1} ---\")\n    result = process_order(order_id=1000 + i, user_id=42)\n    print(f\"Result: {result}\")\n\n# Flush the exporter\nprovider.shutdown()\nPYEOF\n\npython3 /workspace/traced-service.py 2>&1 | head -80\n\n# ── 3. Inspect the span output ────────────────────────────────────────────\n# The console exporter prints JSON spans. Look for:\n#   \"name\": \"process_order\" — the parent span\n#   \"name\": \"db.query\"     — child span with db.statement attribute\n#   \"events\"               — slow query event if latency > 100ms\n\n# ── 4. View trace hierarchy ───────────────────────────────────────────────\n# Spans include parent_id — try to correlate them manually:\npython3 /workspace/traced-service.py 2>&1 | python3 -c \"\nimport sys, json\n\nspans = []\nfor line in sys.stdin:\n    line = line.strip()\n    if line.startswith('{'):\n        try:\n            spans.append(json.loads(line))\n        except:\n            pass\n\nprint(f'\\nCollected {len(spans)} spans:')\nfor s in spans:\n    ctx = s.get('context', {})\n    parent = s.get('parent_id', 'ROOT')\n    name = s.get('name', '?')\n    dur = s.get('end_time', 0) - s.get('start_time', 0)\n    print(f'  [{parent[:8] if parent != \\\"ROOT\\\" else \\\"ROOT    \\\"}] {name:<25} {dur//1000000}ms')\n\"\n\n# ── 5. Add error tracking ─────────────────────────────────────────────────\ncat > /workspace/traced-errors.py << 'PYEOF'\nfrom opentelemetry import trace\nfrom opentelemetry.sdk.trace import TracerProvider\nfrom opentelemetry.sdk.trace.export import ConsoleSpanExporter, SimpleSpanProcessor\n\nprovider = TracerProvider()\nprovider.add_span_processor(SimpleSpanProcessor(ConsoleSpanExporter()))\ntrace.set_tracer_provider(provider)\ntracer = trace.get_tracer(\"demo\")\n\ndef risky_operation():\n    with tracer.start_as_current_span(\"risky_op\") as span:\n        try:\n            raise ValueError(\"database connection refused\")\n        except Exception as e:\n            span.record_exception(e)  # records stack trace in the span\n            span.set_status(trace.StatusCode.ERROR, str(e))\n            raise\n\ntry:\n    risky_operation()\nexcept:\n    pass\n\nprovider.shutdown()\nPYEOF\n\npython3 /workspace/traced-errors.py 2>&1 | python3 -c \"\nimport sys, json\nfor line in sys.stdin:\n    if line.strip().startswith('{'):\n        try:\n            s = json.loads(line.strip())\n            print('Status:', s.get('status'))\n            for ev in s.get('events', []):\n                print('Event:', ev.get('name'))\n                for k, v in ev.get('attributes', {}).items():\n                    if 'stacktrace' in k:\n                        print('Stack trace:', v[:200], '...')\n        except: pass\n\"\n```"
    },
    {
      "id": "quiz",
      "title": "Check your understanding",
      "type": "quiz",
      "questions": [
        {
          "q": "What is the primary advantage of tail-based sampling over head-based sampling?",
          "options": [
            "It uses less memory",
            "It can selectively keep 100% of error traces while dropping fast, successful ones",
            "It is simpler to configure",
            "It works without an OTel Collector"
          ],
          "answer": 1,
          "explanation": "Tail-based sampling makes the sample decision after the trace is complete, so it can keep all traces with errors or high latency while dropping the majority of fast, successful traces. Head-based sampling decides upfront and may discard rare error traces."
        },
        {
          "q": "What does context propagation achieve in distributed tracing?",
          "options": [
            "It compresses span data to reduce network overhead",
            "It allows the OTel Collector to filter spans",
            "It carries the trace_id across service boundaries so all spans belong to the same trace",
            "It authenticates the tracing agent with the backend"
          ],
          "answer": 2,
          "explanation": "Context propagation injects the current trace context (trace_id, span_id) into outgoing HTTP headers (e.g., traceparent). The receiving service extracts these headers and creates a child span, ensuring all work across services is linked under the same trace."
        },
        {
          "q": "Which OTel component is responsible for receiving spans from applications, filtering/batching them, and forwarding to a backend?",
          "options": [
            "TracerProvider",
            "SpanProcessor",
            "OTel Collector",
            "Propagator"
          ],
          "answer": 2,
          "explanation": "The OTel Collector is a standalone service that acts as a telemetry pipeline: it receives spans via OTLP, can filter/sample/transform them, and exports to one or more backends (Jaeger, Tempo, Datadog, etc.)."
        }
      ]
    },
    {
      "id": "challenge",
      "title": "Challenge: Add tracing to a multi-function workflow",
      "type": "challenge",
      "goal": "Extend /workspace/traced-service.py to add a third nested function called `check_inventory(item_id)` that is called inside `process_order()`. The inventory check should simulate a 50ms external HTTP call and add an attribute `inventory.available` (boolean). At least one run should trigger a slow-query event on the db.query span (>100ms).",
      "hints": [
        "Use tracer.start_as_current_span('check_inventory') as a context manager inside process_order()",
        "Increase the random.uniform upper bound on query_database to ensure some runs exceed 100ms",
        "Set a boolean attribute with span.set_attribute('inventory.available', True)"
      ],
      "success_criteria": [
        "A span named 'check_inventory' appears in the console output",
        "The span has an 'inventory.available' attribute",
        "At least one 'db.query' span includes a 'slow query detected' event"
      ]
    }
  ]
}
